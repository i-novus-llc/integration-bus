<?xml version='1.0' encoding='UTF-8'?>
<object xmlns="http://n2oapp.net/framework/config/schema/object-1.0"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://n2oapp.net/framework/config/schema/object-1.0 http://localhost:8080/n2o/config/schema/object/object-1.0.xsd"
        xmlns:sql="http://n2oapp.net/framework/config/schema/action-sql-1.0">
    <name>registry</name>
    <actions>
        <action id="create">
            <invocation>
                <sql:sql>
                    INSERT INTO integration.registry(
                    code, url, name, disable)
                    VALUES (:id, :url, :name, :disable);
                </sql:sql>
            </invocation>
            <in-parameters>
                <param name="id" mapping="['id']" domain="string"/>
                <param name="name" mapping="['name']" domain="string"/>
                <param name="url" mapping="['url']" domain="string"/>
                <param name="disable" mapping="['disable']" domain="boolean"/>
            </in-parameters>
        </action>

        <action id="update">
            <invocation>
                <sql:sql>
                    UPDATE integration.registry
                    SET url=:url, name=:name, disable=:disable
                    WHERE code=:id;
                </sql:sql>
            </invocation>
            <in-parameters>
                <param name="id" mapping="['id']" domain="string"/>
                <param name="name" mapping="['name']" domain="string"/>
                <param name="url" mapping="['url']" domain="string"/>
                <param name="disable" mapping="['disable']" domain="boolean"/>
            </in-parameters>
        </action>
    </actions>
</object>
